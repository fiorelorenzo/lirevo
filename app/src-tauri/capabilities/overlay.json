{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Minimal capability for the always-on-top recording overlay. It only needs to listen for `recording:level` / `recording:state` events and call the `frontend_log` debug command — nothing else. Keeping its surface small means a hypothetical XSS in the overlay webview cannot reach `models_download`, `update_settings`, `pick_file`, etc.",
  "windows": ["overlay"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:webview:allow-internal-toggle-devtools"
  ]
}
