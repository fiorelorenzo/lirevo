{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Full capability set — windows the user actually interacts with (config, model downloads, file picker, etc.). The transparent overlay window uses a minimal capability instead so a compromised webview there can't reach the same surface.",
  "windows": ["main", "home", "wizard", "settings", "model-manager"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:webview:allow-create-webview-window",
    "store:default",
    "dialog:default",
    "shell:allow-open",
    "opener:default",
    "updater:default",
    "process:allow-restart",
    "autostart:default"
  ]
}
