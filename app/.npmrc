# vite 8 requires legacy peer-deps resolution because @sveltejs/kit 2.60 still
# declares a vite ^5 || ^6 peer range despite working fine with vite 7/8.
# Remove when SvelteKit updates its peer range.
legacy-peer-deps=true
