# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Future Node / Electron
node_modules/
dist/
out/
*.log

# Future Rust
target/

# Future Python (MLX sidecar)
__pycache__/
*.pyc
.venv/

# Models (downloaded at runtime, not source)
models/
# Exception: the frontend `models` lib folder is source, not weights.
!app/src/lib/models/
*.bin
*.gguf
*.mlmodelc/

# Builds
build/
release/
*.dmg
*.AppImage
*.exe
.claude/settings.local.json

# Sidecar binary copied into resources at build time
app/resources/inference-core

# Superpowers skills generated docs
docs/plans/
docs/specs/
docs/decisions/
docs/reports/
docs/superpowers/

# Visual Companion brainstorm files (mockups, state)
.superpowers/

# Test audio fixtures (provided locally, not committed)
crates/inference-core/tests/fixtures/*.wav
.claude/

# Local-only env (e.g. APPLE_SIGNING_IDENTITY for dev-bundle signing)
.env

# Local working notes (not source)
notes.md

# Local working notes with secrets — never commit (case-sensitive FS safety)
NOTES.md
